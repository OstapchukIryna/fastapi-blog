# Copy to .env and fill in. core/config.py reads this file; .env itself is
# not in the repository, so without it the application refuses to start.
#
#   cp .env.example .env
#   python -c "import secrets; print(secrets.token_urlsafe(32))"   # SECRET_KEY

# Signs the JWTs. No default on purpose — a shipped default secret is one
# everybody knows. Changing it invalidates every token already issued.
SECRET_KEY=

# MAIL_SERVER=sandbox.smtp.mailtrap.io
# MAIL_PORT=2525
# MAIL_USERNAME=
# MAIL_PASSWORD=
# MAIL_FROM=noreply@called.mad.me
# MAIL_USE_TLS=true
# FRONTEND_URL=http://localhost:8000
# Required — there is no default any more, and no SQLite fallback.
DATABASE_URL=postgresql+psycopg://bloguser:blogpassword@localhost/blog

# The tests use this database with `_test` on the end (blog_test here) and
# drop its schema on every run, so it has to exist and it must not be the
# one above. Create it once:
#
#   createdb blog_test -O bloguser
#   uv run alembic upgrade head        # schema
#   uv run python seed.py              # content
#
# TEST_DATABASE_URL overrides the derivation if the two cannot be neighbours.
