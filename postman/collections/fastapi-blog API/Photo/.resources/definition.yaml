$kind: collection
description: Uploading, replacing and removing the profile picture. Runs after Users, whose account it borrows, and cleans the picture up itself so a run leaves no files behind either.
order: 2200
