$kind: http-request
description: OAuth2PasswordBearer refuses the request before the route runs.
url: "{{baseUrl}}/api/users/me"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test("401 Unauthorized", () => pm.response.to.have.status(401));
    language: text/javascript
order: 5000
